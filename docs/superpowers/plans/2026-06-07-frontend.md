# RepoMentor Frontend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a lightweight, no-login Web Dashboard using Vite + React + Vanilla CSS to interact with the RepoMentor backend.

**Architecture:** A Single Page Application (SPA) with two primary view states (Input and Analysis/Report). State is managed via React hooks. The application uses Server-Sent Events (SSE) to receive real-time updates and dynamically renders the UI (Stage Indicators, Logs, Interactions, and final Reports). It resides in the `web/` directory and directly imports backend types via monorepo path aliases.

**Tech Stack:** Vite, React, TypeScript, Vanilla CSS, Vitest, Testing Library, React Router DOM, React Markdown.

---

### Task 1: Project Scaffold & Testing Setup

**Files:**
- Create: `web/package.json`
- Create: `web/vite.config.ts`
- Create: `web/vitest.setup.ts`
- Create: `web/tsconfig.json`

- [ ] **Step 1: Initialize Vite React TS Project**

```bash
cd web
npm init -y
npm install react react-dom react-router-dom react-markdown
npm install -D typescript @types/react @types/react-dom vite @vitejs/plugin-react vitest jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

- [ ] **Step 2: Configure Vite and Vitest**

Modify `web/vite.config.ts` (Proxy `/analysis` directly and add `resolve.alias` for backend types):
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@backend-types': path.resolve(__dirname, '../src/types')
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/analysis': 'http://127.0.0.1:3000'
    }
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './vitest.setup.ts',
  }
});
```

- [ ] **Step 3: Setup Testing Library**

Create `web/vitest.setup.ts`:
```typescript
import '@testing-library/jest-dom';
```

Modify `web/package.json` to add test scripts:
```json
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run"
  }
```

- [ ] **Step 4: Configure TypeScript for Backend Types**

Create `web/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@backend-types/*": ["../src/types/*"]
    }
  },
  "include": ["src", "vitest.setup.ts"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```
Create `web/tsconfig.node.json`:
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 5: Write a simple sanity test**

Create `web/src/App.test.tsx`:
```typescript
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import App from './App';

describe('App', () => {
  it('renders correctly', () => {
    render(<App />);
    expect(screen.getByText('RepoMentor')).toBeInTheDocument();
  });
});
```

Create `web/src/App.tsx`:
```typescript
export default function App() {
  return <h1>RepoMentor</h1>;
}
```

- [ ] **Step 6: Run test to verify it passes**

Run: `cd web && npm test`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add web/
git commit -m "feat(web): initialize react project with vitest and backend type paths"
```

---

### Task 2: CSS Foundation & Anthropic Design System

**Files:**
- Create: `web/src/index.css`
- Modify: `web/src/main.tsx`

- [ ] **Step 1: Define Global CSS Variables with Font Fallbacks**

Create `web/src/index.css`:
```css
:root {
  --bg-color: #FAF8F5;
  --text-color: #2D2D2D;
  --accent-color: #C2603E;
  --divider-color: #E8E4DF;
  --font-serif: 'Literata', Georgia, serif;
  --font-sans: 'Instrument Sans', system-ui, -apple-system, sans-serif;
}

body {
  margin: 0;
  padding: 0;
  background-color: var(--bg-color);
  color: var(--text-color);
  font-family: var(--font-serif);
  line-height: 1.6;
}

h1, h2, h3, h4, h5, h6, .tag, .badge {
  font-family: var(--font-sans);
}

.container {
  max-width: 1000px;
  margin: 0 auto;
  padding: 2rem;
}

.divider {
  border: none;
  border-top: 1px solid var(--divider-color);
  margin: 2rem 0;
}
```

- [ ] **Step 2: Setup Main Entry**

Create `web/src/main.tsx`:
```typescript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

Create `web/index.html`:
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RepoMentor</title>
    <!-- Add Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Instrument+Sans:wght@400;600&family=Literata:wght@400;600&display=swap" rel="stylesheet">
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 3: Commit**

```bash
git add web/
git commit -m "style(web): add global css variables with font fallbacks"
```

---

### Task 3: API Client

**Files:**
- Create: `web/src/api.ts`
- Create: `web/src/api.test.ts`

- [ ] **Step 1: Write API wrapper tests**

Create `web/src/api.test.ts`:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { createAnalysis } from './api';

global.fetch = vi.fn();

describe('API Client', () => {
  it('createAnalysis posts to /analysis', async () => {
    (fetch as any).mockResolvedValueOnce({
      ok: true,
      json: async () => ({ taskId: '123' })
    });
    
    const res = await createAnalysis('https://github.com/foo/bar', 'main');
    expect(res.taskId).toBe('123');
    expect(fetch).toHaveBeenCalledWith('/analysis', expect.objectContaining({
      method: 'POST'
    }));
  });
});
```

- [ ] **Step 2: Implement API wrapper using correct path**

Create `web/src/api.ts`:
```typescript
import type { CreateAnalysisResponse, GetAnalysisResponse, AskResponse } from '@backend-types/index';

export async function createAnalysis(repoUrl: string, branch: string = 'main'): Promise<CreateAnalysisResponse> {
  const res = await fetch('/analysis', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ repoUrl, branch })
  });
  if (!res.ok) throw new Error('Failed to create analysis');
  return res.json();
}

export async function getAnalysis(taskId: string): Promise<GetAnalysisResponse> {
  const res = await fetch(`/analysis/${taskId}`);
  if (!res.ok) throw new Error('Failed to get analysis');
  return res.json();
}

export async function answerInteraction(taskId: string, questionId: string, answer: string): Promise<AskResponse> {
  const res = await fetch(`/analysis/${taskId}/ask`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ questionId, answer })
  });
  if (!res.ok) throw new Error('Failed to answer interaction');
  return res.json();
}
```

- [ ] **Step 3: Run test to verify it passes**

Run: `cd web && npm test`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add web/
git commit -m "feat(web): add strongly typed rest api client wrappers"
```

---

### Task 4: InputView Component (With Inline Errors)

**Files:**
- Create: `web/src/components/InputView.tsx`
- Create: `web/src/components/InputView.test.tsx`
- Create: `web/src/components/InputView.css`

- [ ] **Step 1: Write InputView tests**

Create `web/src/components/InputView.test.tsx`:
```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import { describe, it, expect, vi } from 'vitest';
import { InputView } from './InputView';

describe('InputView', () => {
  it('calls onSubmit and shows inline error if passed', () => {
    const submitSpy = vi.fn();
    render(<InputView onSubmit={submitSpy} error="Failed to connect" />);
    
    expect(screen.getByText('Failed to connect')).toBeInTheDocument();
    
    const urlInput = screen.getByPlaceholderText(/https:\/\/github.com/);
    fireEvent.change(urlInput, { target: { value: 'test/repo' } });
    
    const btn = screen.getByRole('button', { name: /Analyze/i });
    fireEvent.click(btn);
    
    expect(submitSpy).toHaveBeenCalledWith('test/repo', 'main');
  });
});
```

- [ ] **Step 2: Implement InputView with inline errors**

Create `web/src/components/InputView.css`:
```css
.input-view {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
  text-align: center;
}
.input-form {
  display: flex;
  gap: 1rem;
  width: 100%;
  max-width: 600px;
}
.input-url { flex: 1; padding: 0.75rem 1rem; border: 1px solid var(--divider-color); }
.input-branch { width: 120px; padding: 0.75rem; border: 1px solid var(--divider-color); }
.btn-primary { background: var(--accent-color); color: white; border: none; padding: 0.75rem 1.5rem; cursor: pointer; }
.error-text { color: red; margin-top: 1rem; font-family: var(--font-sans); }
```

Create `web/src/components/InputView.tsx`:
```typescript
import { useState } from 'react';
import './InputView.css';

interface Props {
  onSubmit: (url: string, branch: string) => void;
  error?: string | null;
}

export function InputView({ onSubmit, error }: Props) {
  const [url, setUrl] = useState('');
  const [branch, setBranch] = useState('main');

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (url.trim()) onSubmit(url.trim(), branch.trim() || 'main');
  };

  return (
    <div className="input-view">
      <h1>RepoMentor</h1>
      <p className="tagline">Deep structural analysis for any codebase.</p>
      <form className="input-form" onSubmit={handleSubmit}>
        <input className="input-url" type="text" placeholder="https://github.com/owner/repo" value={url} onChange={(e) => setUrl(e.target.value)} />
        <input className="input-branch" type="text" placeholder="branch (main)" value={branch} onChange={(e) => setBranch(e.target.value)} />
        <button className="btn-primary" type="submit">Analyze</button>
      </form>
      {error && <p className="error-text">{error}</p>}
    </div>
  );
}
```

- [ ] **Step 3: Run test and commit**

```bash
cd web && npm test
git add web/
git commit -m "feat(web): build InputView component with inline error handling"
```

---

### Task 5: useAnalysisStream Hook (Fully Typed + Timeout Handling)

**Files:**
- Create: `web/src/hooks/useAnalysisStream.ts`
- Create: `web/src/hooks/useAnalysisStream.test.ts`
- Modify: `web/src/App.tsx`

- [ ] **Step 1: Write Hook tests**

Create `web/src/hooks/useAnalysisStream.test.ts`:
```typescript
import { renderHook } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { useAnalysisStream } from './useAnalysisStream';

describe('useAnalysisStream', () => {
  it('initializes default state and exports clearInteraction', () => {
    const { result } = renderHook(() => useAnalysisStream('task-123'));
    expect(result.current.state.status).toBe('cloning');
    expect(typeof result.current.clearInteraction).toBe('function');
  });
});
```

- [ ] **Step 2: Implement robust hook with proper event filtering and timeout reset**

Create `web/src/hooks/useAnalysisStream.ts`:
```typescript
import { useState, useEffect, useCallback } from 'react';
import type { TaskStatus, StageProgress, SSEEvent, TaskError, AnalysisResult } from '@backend-types/index';

export interface StreamState {
  status: TaskStatus;
  stageProgress: StageProgress;
  logs: string[];
  interaction: { id: string; question: string; options: string[] } | null;
  result: Partial<AnalysisResult>;
  error: string | null;
}

export function useAnalysisStream(taskId: string | null) {
  const [state, setState] = useState<StreamState>({
    status: 'cloning',
    stageProgress: { explorer: 'pending', mentor: 'pending', contributor: 'pending' },
    logs: [],
    interaction: null,
    result: {},
    error: null
  });

  const clearInteraction = useCallback(() => {
    setState(s => ({ ...s, interaction: null }));
  }, []);

  useEffect(() => {
    if (!taskId) return;

    const eventSource = new EventSource(`/analysis/${taskId}/stream`);

    const handleEvent = (e: MessageEvent) => {
      try {
        const event = JSON.parse(e.data) as SSEEvent;
        
        switch (event.type) {
          case 'task:created':
            setState(s => ({ ...s, status: event.status }));
            break;
          case 'task:error':
            setState(s => ({ ...s, error: event.error.message }));
            eventSource.close();
            break;
          case 'stage:progress':
            setState(s => ({ ...s, logs: [...s.logs, event.message] }));
            break;
          case 'stage:start':
            setState(s => ({ ...s, stageProgress: { ...s.stageProgress, [event.stage]: 'running' } }));
            break;
          case 'stage:done':
            setState(s => ({ 
              ...s, 
              stageProgress: { ...s.stageProgress, [event.stage]: 'done' },
              result: { ...s.result, [event.stage]: event.output }
            }));
            break;
          case 'interact:ask':
            setState(s => ({ ...s, interaction: { id: event.questionId, question: event.question, options: event.options || [] } }));
            break;
          case 'interact:timeout':
            setState(s => ({ ...s, interaction: null }));
            break;
          case 'task:completed':
            setState(s => ({ ...s, status: 'completed' }));
            eventSource.close();
            break;
        }
      } catch (err) {
        console.error('SSE parse error:', err);
      }
    };

    // The backend only sends named events. We bind to specific event names.
    const eventTypes = ['task:created', 'task:error', 'stage:progress', 'stage:start', 'stage:done', 'interact:ask', 'interact:timeout', 'task:completed'];
    eventTypes.forEach(type => eventSource.addEventListener(type, handleEvent));

    return () => eventSource.close();
  }, [taskId]);

  return { state, clearInteraction };
}
```

- [ ] **Step 3: Connect App State**

Modify `web/src/App.tsx`:
```typescript
import { useState } from 'react';
import { InputView } from './components/InputView';
import { createAnalysis } from './api';
import { useAnalysisStream } from './hooks/useAnalysisStream';

export default function App() {
  const [taskId, setTaskId] = useState<string | null>(null);
  const [submitError, setSubmitError] = useState<string | null>(null);
  const { state: streamState } = useAnalysisStream(taskId);

  const handleAnalyze = async (url: string, branch: string) => {
    setSubmitError(null);
    try {
      const res = await createAnalysis(url, branch);
      setTaskId(res.taskId);
    } catch (err) {
      setSubmitError(err instanceof Error ? err.message : 'Failed to start analysis');
    }
  };

  if (!taskId) {
    return (
      <div className="container">
        <InputView onSubmit={handleAnalyze} error={submitError} />
      </div>
    );
  }

  return (
    <div className="container">
      <h2>Analysis Task: {taskId}</h2>
      <p>Status: {streamState.status}</p>
      {streamState.error && <div style={{color: 'red'}}>{streamState.error}</div>}
      <pre>{JSON.stringify(streamState.stageProgress, null, 2)}</pre>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add web/
git commit -m "feat(web): add typed useAnalysisStream hook with timeout reset and correct listener binding"
```

---

### Task 6: ProgressUI & Interaction (With Loading States)

**Files:**
- Create: `web/src/components/ProgressUI.tsx`
- Create: `web/src/components/ProgressUI.css`
- Create: `web/src/components/ProgressUI.test.tsx`
- Modify: `web/src/App.tsx`

- [ ] **Step 1: Write ProgressUI component with loading states**

Create `web/src/components/ProgressUI.css`:
```css
.stage-indicator { display: flex; gap: 2rem; margin-bottom: 2rem; }
.stage-card { flex: 1; padding: 1.5rem; border: 1px solid var(--divider-color); background: white; border-radius: 8px; }
.stage-card.running { border-color: var(--accent-color); box-shadow: 0 4px 12px rgba(194, 96, 62, 0.1); }
.stage-card.done { border-color: #A3C2A0; }
.log-panel { background: #1E1E1E; color: #D4D4D4; padding: 1rem; border-radius: 4px; font-family: monospace; max-height: 200px; overflow-y: auto; font-size: 0.85rem; }
.interaction-box { margin-top: 1rem; padding: 1rem; background: #FFF9F0; border: 1px solid var(--accent-color); border-radius: 4px; }
.interaction-options { display: flex; gap: 1rem; margin-top: 1rem; }
button:disabled { opacity: 0.6; cursor: not-allowed; }
```

Create `web/src/components/ProgressUI.tsx`:
```typescript
import { useState } from 'react';
import type { StageProgress } from '@backend-types/index';
import './ProgressUI.css';

interface ProgressProps {
  stages: StageProgress;
  logs: string[];
  interaction: { id: string; question: string; options: string[] } | null;
  onAnswer: (id: string, answer: string) => Promise<void>;
}

export function ProgressUI({ stages, logs, interaction, onAnswer }: ProgressProps) {
  const [submitting, setSubmitting] = useState(false);
  const stageList: (keyof StageProgress)[] = ['explorer', 'mentor', 'contributor'];
  
  const handleAnswer = async (id: string, answer: string) => {
    setSubmitting(true);
    await onAnswer(id, answer);
    setSubmitting(false);
  };

  return (
    <div>
      <div className="stage-indicator">
        {stageList.map(s => (
          <div key={s} className={`stage-card ${stages[s]}`}>
            <h3>{s.charAt(0).toUpperCase() + s.slice(1)}</h3>
            <p className="tag">{stages[s]}</p>
          </div>
        ))}
      </div>
      
      {interaction && (
        <div className="interaction-box">
          <p><strong>Question:</strong> {interaction.question}</p>
          <div className="interaction-options">
            {interaction.options.map(opt => (
              <button key={opt} disabled={submitting} className="btn-primary" onClick={() => handleAnswer(interaction.id, opt)}>
                {submitting ? 'Sending...' : opt}
              </button>
            ))}
          </div>
        </div>
      )}

      {logs.length > 0 && (
        <details>
          <summary style={{cursor: 'pointer', margin: '1rem 0', fontFamily: 'var(--font-sans)'}}>
            Detailed Logs ({logs.length})
          </summary>
          <div className="log-panel">
            {logs.slice(-50).map((log, i) => <div key={i}>{log}</div>)}
          </div>
        </details>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Connect ProgressUI in App and clear interaction after answer**

Modify `web/src/App.tsx` (replace `<pre>` output):
```typescript
// Add imports:
// import { ProgressUI } from './components/ProgressUI';
// import { answerInteraction } from './api';

  const { state: streamState, clearInteraction } = useAnalysisStream(taskId);

  const handleAnswer = async (id: string, answer: string) => {
    if (!taskId) return;
    try {
      await answerInteraction(taskId, id, answer);
      clearInteraction(); // Optimistically clear interaction after successful send
    } catch (err) {
      console.error(err);
    }
  };

// Replace render content:
  return (
    <div className="container">
      <h2>Analysis Task: {taskId}</h2>
      {streamState.error && <div style={{color: 'red'}}>{streamState.error}</div>}
      
      {streamState.status !== 'completed' && (
        <ProgressUI 
          stages={streamState.stageProgress}
          logs={streamState.logs}
          interaction={streamState.interaction}
          onAnswer={handleAnswer}
        />
      )}

      {streamState.status === 'completed' && (
        <div style={{marginTop: '3rem'}}>
          <h3>Analysis Complete</h3>
        </div>
      )}
    </div>
  );
```

- [ ] **Step 3: Run test and Commit**

```bash
cd web && npm run build
git add web/
git commit -m "feat(web): build ProgressUI with loading state and connection to App interaction clearing"
```

---

### Task 7: AnchorNav & Report Sections (With Markdown)

**Files:**
- Create: `web/src/components/ReportSections.tsx`
- Create: `web/src/components/ReportSections.css`
- Modify: `web/src/App.tsx`

- [ ] **Step 1: Build typed Report Sections with AnchorNav & Markdown**

Create `web/src/components/ReportSections.css`:
```css
.anchor-nav {
  position: sticky;
  top: 0;
  background: var(--bg-color);
  padding: 1rem 0;
  border-bottom: 1px solid var(--divider-color);
  display: flex;
  gap: 2rem;
  z-index: 100;
  margin-bottom: 2rem;
}
.anchor-nav a {
  color: var(--text-color);
  text-decoration: none;
  font-family: var(--font-sans);
  font-weight: 600;
}
.anchor-nav a:hover { color: var(--accent-color); }
html { scroll-behavior: smooth; }

.report-section { margin-bottom: 4rem; padding-top: 2rem; }
.report-section h2 { font-size: 2rem; border-bottom: 1px solid var(--divider-color); padding-bottom: 1rem; margin-bottom: 2rem; }
.grid-cards { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.5rem; }
.card { padding: 1.5rem; border: 1px solid var(--divider-color); background: white; border-radius: 4px; }
.badge { background: #EEE; padding: 0.2rem 0.6rem; border-radius: 12px; font-size: 0.8rem; margin-right: 0.5rem; font-family: var(--font-sans); }
.markdown-body { font-family: var(--font-serif); line-height: 1.6; }
.markdown-body pre { background: #1E1E1E; color: #D4D4D4; padding: 1rem; overflow-x: auto; font-family: monospace; }
.markdown-body code { background: #EEE; padding: 0.1rem 0.3rem; border-radius: 3px; font-family: monospace; }
```

Create `web/src/components/ReportSections.tsx`:
```typescript
import ReactMarkdown from 'react-markdown';
import type { ExplorerOutput, MentorOutput, ContributorOutput } from '@backend-types/index';
import './ReportSections.css';

export function AnchorNav() {
  return (
    <nav className="anchor-nav">
      <a href="#overview">Overview</a>
      <a href="#architecture">Architecture</a>
      <a href="#contribute">Contribute</a>
    </nav>
  );
}

export function ExplorerSection({ data }: { data?: ExplorerOutput }) {
  if (!data) return null;
  return (
    <section className="report-section" id="overview">
      <h2>Overview</h2>
      <p style={{fontSize: '1.2rem'}}>{data.projectSummary}</p>
      
      <div style={{margin: '2rem 0'}}>
        <span className="badge">{data.projectType.primary}</span>
        <span className="badge">{data.techStack.language}</span>
        {data.techStack.framework && <span className="badge">{data.techStack.framework}</span>}
      </div>

      <h3>Module Map</h3>
      <div className="grid-cards">
        {data.moduleMap.map((mod, i) => (
          <div key={i} className="card">
            <h4>{mod.path}</h4>
            <p>{mod.responsibility}</p>
            <span className="badge">{mod.importance}</span>
          </div>
        ))}
      </div>
    </section>
  );
}

export function MentorSection({ data }: { data?: MentorOutput }) {
  if (!data) return null;
  return (
    <section className="report-section" id="architecture">
      <h2>Architecture</h2>
      <div className="card" style={{background: '#FAF8F5'}}>
        <ReactMarkdown className="markdown-body">{data.architectureOverview}</ReactMarkdown>
      </div>

      <h3 style={{marginTop: '2rem'}}>Reading Path</h3>
      <ol>
        {data.readingPath.map((step, i) => (
          <li key={i} style={{marginBottom: '1rem'}}>
            <strong>{step.file}</strong>: {step.why}
          </li>
        ))}
      </ol>
    </section>
  );
}

export function ContributorSection({ data }: { data?: ContributorOutput }) {
  if (!data) return null;
  return (
    <section className="report-section" id="contribute">
      <h2>Contribute</h2>
      <p><strong>Setup:</strong> {data.contributionSetup.devEnv}</p>
      
      <h3 style={{marginTop: '2rem'}}>Good First Issues</h3>
      <div className="grid-cards">
        {data.goodFirstIssues.map((issue, i) => (
          <div key={i} className="card">
            <h4>{issue.area}</h4>
            <span className="badge" style={{color: 'var(--accent-color)'}}>{issue.difficulty}</span>
            <p style={{marginTop: '1rem'}}>{issue.description}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Connect Reports to App**

Modify `web/src/App.tsx` completed state block:
```typescript
// Add these imports at the top:
// import { AnchorNav, ExplorerSection, MentorSection, ContributorSection } from './components/ReportSections';
// import type { ExplorerOutput, MentorOutput, ContributorOutput } from '@backend-types/index';

      {streamState.status === 'completed' && (
        <div style={{marginTop: '1rem'}}>
          <AnchorNav />
          <ExplorerSection data={streamState.result?.explorer as ExplorerOutput} />
          <MentorSection data={streamState.result?.mentor as MentorOutput} />
          <ContributorSection data={streamState.result?.contributor as ContributorOutput} />
        </div>
      )}
```

- [ ] **Step 3: Run tests to ensure build passes**

Run: `cd web && npm run build`
Expected: Passes.

- [ ] **Step 4: Commit**

```bash
git add web/
git commit -m "feat(web): build typed report sections with AnchorNav and Markdown support"
```
