{
  "name": "mini-repo",
  "version": "1.0.0",
  "description": "A minimal test fixture for RepoMentor integration tests",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
